name: Test BPF

# Any change in triggers needs to be reflected in the concurrency group.
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main


concurrency:
  # Structure:
  # - Workflow name
  # - Event type
  # - A unique identifier depending on event type:
  #   - schedule: SHA
  #   - workflow_dispatch: PR number
  #
  # This structure ensures a unique concurrency group name is generated for each
  # type of testing, such that re-runs will cancel the previous run.
  group: |
    ${{ github.workflow }}
    ${{ github.event_name }}
    ${{
      (github.event_name == 'schedule' && github.sha) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.PR-number)
    }}
  cancel-in-progress: true

env:
  # renovate: datasource=golang-version depName=go
  go-version: 1.26.1

jobs:
  setup-and-test:
    permissions:
      checks: write
    runs-on: 'ubuntu-24.04'
    name: Setup & Test
    strategy:
      fail-fast: false
      matrix:
        include:
          - kernel: '6.1'
          - kernel: '6.6'
          - kernel: '6.12'
          - kernel: '6.18'
          - kernel: 'bpf-next'
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Resolve full kernel version
        id: kernel-version
        run: |
          KERNEL="${{ matrix.kernel }}"
          case "$KERNEL" in
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "5.15") FULL_KERNEL="5.15-20260525.014435" ;;
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "6.1") FULL_KERNEL="6.1-20260525.014435" ;;
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "6.6") FULL_KERNEL="6.6-20260525.014435" ;;
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "6.12") FULL_KERNEL="6.12-20260525.014435" ;;
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "6.18") FULL_KERNEL="6.18-20260525.014435" ;;
            # renovate: datasource=docker depName=quay.io/lvh-images/complexity-test
            "bpf-next") FULL_KERNEL="bpf-next-20260525.014435" ;;
            *) FULL_KERNEL="$KERNEL" ;;
          esac
          echo "full=${FULL_KERNEL}" >> $GITHUB_OUTPUT

      - name: Provision LVH VMs
        uses: cilium/little-vm-helper@v0.0.30
        with:
          test-name: bpf
          image: 'complexity-test'
          image-version: ${{ steps.kernel-version.outputs.full }}
          host-mount: ./
          images-folder-parent: "/tmp"
          cpu: 4
          # renovate: datasource=github-tags depName=cilium/little-vm-helper
          lvh-version: "v0.0.30"
          install-dependencies: 'true'
          cmd: |
            for i in {1..5}; do curl "https://golang.org" > /dev/null 2>&1 && break || sleep 5; echo "Waiting for systemd-resolved to be ready..."; done

            git config --global --add safe.directory /host

            # Use /var/tmp for Go build temporary files to avoid running out
            # of disk space on the tmpfs-backed /tmp in the VM.
            export TMPDIR=/var/tmp

            # The LVH image might ship with an arbitrary Go toolchain version,
            # install the same Go toolchain version as current HEAD.
            CGO_ENABLED=0 GOPROXY=direct GOSUMDB= go install golang.org/dl/go${{ env.go-version }}@latest
            go${{ env.go-version }} download

            # Free up disk space: remove the pre-installed Go toolchain and
            # build caches, only go${{ env.go-version }} is needed from now on.
            go clean -cache -modcache
            rm -rf /usr/local/go
            go${{ env.go-version }} install gotest.tools/gotestsum@latest

      - name: Run bpf tests
        uses: cilium/little-vm-helper@v0.0.30
        with:
          provision: 'false'
          cmd: |
            cd /host
            export TMPDIR=/var/tmp
            export GOCACHE=/host/gocache
            mkdir -p /host/gocache
            # Run with cgo disabled, LVH images don't ship with gcc.
            CGO_ENABLED=0 GOVERSION=${{ env.go-version }} gotestsum --raw-command --junitfile test-results.xml -- go${{ env.go-version }} test -json -v -timeout=5m ./internal/bpf/...

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: success() || failure() # always run even if the previous step fails
        with:
          report_paths: 'test-*.xml'
          check_name: "BPF Test Results ${{ matrix.kernel }}"

      - name: Publish Verifier error logs as artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: verifier-log
          path: /tmp/verifier-*.log
          if-no-files-found: ignore
          archive: false
